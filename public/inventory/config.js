// GlutenFreeInventory configuration.
// The Bungie API key is NOT stored in the repo. Paste it once in the page's
// settings box (saved only in this browser's localStorage) or set it below
// for a private deployment. Bungie API keys are visible to any browser that
// loads the page; lock the key to https://kgffc.net at bungie.net/en/Application.
export const CONFIG = {
  apiKey: "",                 // leave blank to use the in-page settings box
  bungieName: "Exu#6179",     // prefills the box
  manifestLanguage: "en",
};
